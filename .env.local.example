# Google Drive Sync Configuration
# To enable Google Drive sync, you need to create a Google OAuth client ID:
# 1. Go to Google Cloud Console (console.cloud.google.com)
# 2. Create a new project or select existing one
# 3. Enable Google Drive API
# 4. Create OAuth 2.0 client ID (Web application)
# 5. Add your domain to authorized origins (e.g., http://localhost:3000 for development)
# 6. Copy the client ID below

REACT_APP_GOOGLE_CLIENT_ID=your_google_oauth_client_id_here

# For production, also set:
# REACT_APP_GOOGLE_CLIENT_ID=your_production_client_id